def rollDice(sides):
  import random
  result=random.randint(1, sides)
  print("You rolled a", result)
  

while True:
  sides = int(input("How many sides does it have?"))
  rollDice(sides)
  again=input("Do you want to roll again?")
  if again == "yes":
    continue
  else:
    break
